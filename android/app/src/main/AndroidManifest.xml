<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cse112goldteam.robobetty" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".BusinessLoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".NoAppointment" />
        <activity android:name=".FindAppointmentActivity" />
        <activity android:name=".SignatureActivity" />
        <activity android:name=".DoneActivity" />
        <activity
            android:name=".CustomFormActivity"
            android:label="@string/title_activity_custom_form" >
        </activity>
        <activity
            android:name=".FoundActivity"
            android:label="@string/title_activity_found" >
        </activity>
    </application>

</manifest>
